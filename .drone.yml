workspace:
  base: /go
  path: /clone/chef-repo/
pipeline:
  s3:
    image: plugins/s3
    bucket: my-first-drone-pipeline
    secrets: [ aws_access_key_id, aws_secret_access_key ]
    source: name.txt
    target: /my-first-drone-pipeline/files/
    region: us-east-2
